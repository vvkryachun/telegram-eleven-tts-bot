# 🎙️ Telegram Bot для озвучивания текста через ElevenLabs

Telegram-бот для преобразования текста в речь с использованием ElevenLabs API. Поддерживает 7 женских голосов с разными тонами, все голоса говорят на русском языке.

## 🎯 Возможности

- ✅ Преобразование текста в речь на русском языке
- ✅ 7 женских голосов с разными тонами на выбор
- ✅ Автоматическое сохранение аудиофайлов локально
- ✅ Выбор голоса через удобный интерфейс с кнопками
- ✅ Автоматическая синхронизация команд бота
- ✅ Детальная обработка ошибок с понятными сообщениями
- ✅ Безопасное хранение секретов в `.env` файле

## 📋 Требования

- Python 3.8 или выше (рекомендуется 3.10+)
- Telegram Bot Token (получить у [@BotFather](https://t.me/BotFather))
- ElevenLabs API Key (получить на [elevenlabs.io](https://elevenlabs.io))

## 🚀 Установка

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd telegram-eleven-tts-bot
```

### 2. Создание виртуального окружения

```bash
python3 -m venv venv
source venv/bin/activate  # Для Linux/Mac
# или
venv\Scripts\activate  # Для Windows
```

### 3. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 4. Настройка переменных окружения

Скопируйте файл `env.example` в `.env`:

```bash
cp env.example .env
```

Откройте `.env` и заполните ваши данные:

```env
TELEGRAM_BOT_TOKEN=ваш_токен_от_BotFather
ELEVENLABS_API_KEY=ваш_ключ_от_ElevenLabs
```

**Как получить токены:**

- **Telegram Bot Token**: 
  1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
  2. Отправьте команду `/newbot`
  3. Следуйте инструкциям
  4. Скопируйте полученный токен

- **ElevenLabs API Key**:
  1. Зарегистрируйтесь на [elevenlabs.io](https://elevenlabs.io)
  2. Перейдите в настройки аккаунта
  3. Скопируйте API ключ

## 🎮 Использование

### Запуск бота

```bash
python main.py
```

После запуска вы увидите сообщение:
```
Бот запущен и готов к работе!
```

### Команды бота

- `/start` - Начать работу с ботом
- `/help` - Показать справку по использованию
- `/voice` - Выбрать голос для озвучивания
- `/status` - Проверить статус API и текущий голос

### Как использовать

1. Откройте бота в Telegram
2. Отправьте команду `/voice` для выбора голоса
3. Выберите понравившийся голос из списка
4. Отправьте любой текст на русском языке
5. Получите аудиофайл с озвучкой!

## 🎤 Доступные голоса

Бот поддерживает 7 женских голосов с разными тонами:

| Голос | Описание |
|-------|----------|
| **Bella** | Мягкий, спокойный, умиротворяющий |
| **Rachel** | Профессиональный, нейтральный, четкий |
| **Domi** | Энергичный, молодой, динамичный |
| **Elli** | Дружелюбный, теплый, приветливый |
| **Dorothy** | Зрелый, уверенный, авторитетный |
| **Charlotte** | Элегантный, утонченный |
| **Alice** | Нежный, мягкий, деликатный |

Все голоса поддерживают русский язык и используют мультиязычную модель `eleven_multilingual_v2`.

## 📁 Структура проекта

```
telegram-eleven-tts-bot/
├── main.py              # Основной файл бота
├── config.py            # Конфигурация и настройки
├── voice.py             # Модуль работы с ElevenLabs API
├── requirements.txt     # Зависимости проекта
├── .env                 # Секреты (не коммитится в git)
├── env.example          # Пример файла с переменными окружения
├── .gitignore           # Игнорируемые файлы
├── audio/               # Папка для сохранения аудиофайлов
└── README.md            # Этот файл
```

## ⚙️ Настройки

### Максимальная длина текста

По умолчанию: **5000 символов**

Изменить можно в `config.py`:
```python
MAX_TEXT_LENGTH = 5000  # Измените на нужное значение
```

### Голос по умолчанию

По умолчанию используется голос **Bella**.

Изменить можно в `.env`:
```env
ELEVENLABS_VOICE_ID=ID_голоса
```

Или в `config.py` изменив `DEFAULT_VOICE_ID`.

### Настройки генерации речи

В `config.py` можно настроить параметры голоса:

```python
VOICE_SETTINGS = {
    "stability": 0.5,          # Стабильность (0.0-1.0)
    "similarity_boost": 0.75, # Схожесть с оригиналом (0.0-1.0)
    "style": 0.0,             # Стиль произношения (0.0-1.0)
    "use_speaker_boost": True # Улучшение качества
}
```

## 🔧 Решение проблем

### Ошибка: "TELEGRAM_BOT_TOKEN не найден в .env файле"

**Решение**: Убедитесь, что:
- Файл `.env` существует в корне проекта
- В файле указан `TELEGRAM_BOT_TOKEN=ваш_токен`
- Токен указан без кавычек

### Ошибка: "Ошибка авторизации (401)"

**Решение**: 
- Проверьте правильность `ELEVENLABS_API_KEY` в `.env`
- Убедитесь, что API ключ активен на сайте elevenlabs.io

### Ошибка: "Доступ запрещен (403)"

**Возможные причины**:
- Исчерпан лимит бесплатного тарифа
- Аккаунт заблокирован
- Требуется платная подписка

**Решение**:
- Проверьте статус аккаунта на [elevenlabs.io](https://elevenlabs.io)
- Рассмотрите переход на платный тариф

### Ошибка: "Unusual activity detected"

**Причина**: Бесплатный тариф заблокирован из-за необычной активности

**Решение**:
- Проверьте аккаунт на сайте elevenlabs.io
- Убедитесь, что не используется VPN/прокси
- Рассмотрите переход на платный тариф

### Ошибка: "Превышен лимит запросов (429)"

**Решение**: Подождите немного и попробуйте снова. Лимиты зависят от вашего тарифа ElevenLabs.

### Бот не отвечает

**Решение**:
- Проверьте, что бот запущен (`python main.py`)
- Убедитесь, что токен правильный
- Проверьте интернет-соединение
- Посмотрите логи на наличие ошибок

## 📝 Логирование

Бот ведет подробные логи всех операций. Уровень логирования можно изменить в `main.py`:

```python
logging.basicConfig(
    level=logging.INFO  # Измените на DEBUG для более детальных логов
)
```

## 🔒 Безопасность

- ✅ Все секреты хранятся в `.env` файле
- ✅ `.env` добавлен в `.gitignore` и не коммитится в git
- ✅ Используйте `env.example` как шаблон для других разработчиков

**Важно**: Никогда не публикуйте файл `.env` с реальными токенами!

## 📄 Лицензия

Этот проект создан в учебных целях. Используйте на свой страх и риск.

## 🤝 Вклад в проект

Если вы хотите улучшить проект:

1. Создайте форк репозитория
2. Создайте ветку для ваших изменений
3. Внесите изменения
4. Создайте Pull Request

## 📞 Поддержка

Если у вас возникли вопросы или проблемы:

1. Проверьте раздел "Решение проблем" выше
2. Проверьте логи бота
3. Убедитесь, что все зависимости установлены
4. Проверьте статус API на сайтах Telegram и ElevenLabs

## 🎉 Благодарности

- [python-telegram-bot](https://github.com/python-telegram-bot/python-telegram-bot) - библиотека для работы с Telegram Bot API
- [ElevenLabs](https://elevenlabs.io) - сервис для генерации речи
- [python-dotenv](https://github.com/theskumar/python-dotenv) - для работы с переменными окружения

---

**Приятного использования! 🚀**

